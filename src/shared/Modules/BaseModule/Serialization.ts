import Vector3D from "shared/Modules/Libraries/Vector3D";
import parseXML from "shared/Modules/Libraries/XMLParser";

import BaseModule from ".";
import RigidBody from "./RigidBody";
import CraftModule from "./CraftModule";
import CraftPart from "./CraftPart";
import Craft from "./Craft";
import UniverseInstance from "./UniverseInstance";

export default class Serialization extends BaseModule {
    // Private methods

    // Public methods

    // public serializeUniverse(universe: Universe): string {
    // }

    // public deserializeUniverse(xml: string): UniverseInstance {
    // }

    // public serializeCraft(craft: Craft): string {
    // }

    // public deserializeCraft(xml: string): Craft {
    //     const object = parseXML(xml);
    //     return new Craft(
    //         CraftPart.make()
    //     )
    // }
}
